const ONE_WEEK_IN_SECONDS = 7 * 24 * 60 * 60;

export const SUPPLIER_JWT_EXPIRES_IN_SECONDS = ONE_WEEK_IN_SECONDS;

export const SUPPLIER_JWT_REQUEST_KEY = 'supplier';

export const SUPPLIER_PASSWORD_SALT_ROUNDS = 10;

export const FIVE_MB_IN_BYTES = 1024 * 1024 * 5;

export const ONE_MB_IN_BYTES = 1024 * 1024 * 1;

export const ALLOWED_PHOTO_MIME_TYPES = [
  'image/jpeg',
  'image/png',
  'image/webp',
];

export const ALLOWED_CERTIFICATE_MIME_TYPES = [
  'image/jpeg',
  'image/png',
  'image/webp',
  'application/pdf',
];
