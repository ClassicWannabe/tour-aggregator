import React from "react"
import { Tour } from "@/lib/interfaces/tours"

type Props = {
  tour: Tour
}

const TourContactInfo: React.FC<Props> = ({ tour }) => {
  return <article className="bg-yellowAccent rounded-lg p-4">
    
  </article>
}

export default TourContactInfo
